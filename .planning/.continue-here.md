---
context: default
phase: 14-today-calendar-navigation
task: 6
total_tasks: 6
status: in_progress
last_updated: 2026-08-29T12:39:48.493Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **A second writer is in this working tree.** Another agent or session is
  writing phase-15 (Insights) code into the same checkout — `lib/data/insights.ts`,
  `lib/domain/insights/`, `app/api/insights/`, `app/api/settlements/[period]/`, and an
  uncommitted edit to `lib/data/settlement.ts`. None of it is this session's work.
  **Structural mitigation: never run `git add -A` or `git add .` in this repository until
  that is resolved. Stage explicit paths only.** Two files were already moved aside and
  restored once this session to get a verification run; that is a race, not a workflow.

- [ ] CONSTRAINT: **The app does not start right now.**
  `app/api/settlements/[period]/` collides with the existing `app/api/settlements/[id]/`
  and Next refuses to boot: *"You cannot use different slug names for the same dynamic
  path ('id' !== 'period')"*. `npm run dev`, `npm run build` and the entire Playwright
  suite are blocked while that path exists. **Structural mitigation: resolve the slug
  collision with whoever owns the phase-15 work before claiming any verification result.**

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Blanket staging | `git add -A` in a tree a second writer is using sweeps their half-written files into your commit | blocking | Stage explicit paths. Check `git status --short` and match every entry against your own session's work before staging. |
| Reading a red check as your own failure | `npm run typecheck`, `lint` and `build` are all red, and every error is inside the concurrent phase-15 files | advisory | Filter before concluding: `npm run typecheck 2>&1 \| grep -v insights` returns zero errors. |
| Treating an environment outage as a code defect | Docker Desktop stopped mid-session; every signup then returned `INTERNAL` / `TypeError: fetch failed`, which reads exactly like a broken signup route | advisory | On `fetch failed` from any Supabase call, run `npx supabase status` before opening the route handler. |
| Papering over an e2e failure with a wait | Three long-standing journey failures were each a real defect — a missing `min-w-0`, a pre-hydration native form submit, an overlapping date input | advisory | Probe the DOM (min-content widths, the accessibility snapshot's `[disabled]` markers) before adding a timeout. |

<current_state>
Phase 14 — Today, Calendar and navigation — is functionally complete and committed in
five slices, plus a sixth commit of repo-wide fixes. Every slice was verified green at
the moment it landed.

What is left is documentation, not code: `PROGRESS.md` and `DECISIONS.md` have not been
updated for phase 14, and one small already-written change to
`components/forms/join-request-form.tsx` is uncommitted.

The working tree cannot currently build or run, entirely because of the concurrent
phase-15 work described in the constraints above.
</current_state>

<completed_work>

Committed, newest first:

- `a3cafa8` — repo-wide fixes: the 360 px overflow, the invite-link hydration guard,
  the Plan-it wrap, e2e cleanup and Playwright config
- `89659f1` — phase-14 Playwright journey, `tests/e2e/today.spec.ts`, 9 cases
- `f4d30cb` — the six-item bar, the universal quick-add, the desktop sidebar
- `c92d46a` — S-52 Calendar day/week/month and `/api/calendar/{day,week,month}`
- `b9b6e54` — S-50 Today, `GET /api/today`, house announcements (migration 089)
- `95be544` — S-51 Home overview at `/home`, `/dashboard` retired to a redirect

New tests, all passing: 15 unit cases for the Calendar arithmetic, 12 for Today's
"Needs you" ordering and presence, 10 for the Home overview's pending queues and
who-owes-whom, 5 for the quick-add's privilege rule, 11 integration cases against a real
Postgres for announcements, and the 9-case phase journey. The suite went from 741 to 794
passing across 60 files, with nothing skipped.

Three failures that `PROGRESS.md` had recorded as open were root-caused and fixed:

- **`/house/members` overflowing a 360 px screen by 177 px.** The app shell's content
  column is a flex item, so its default `min-width: auto` floored it at its own
  min-content width — and every `truncate` in the app carries `white-space: nowrap`,
  which contributes a whole unwrapped line to that figure. One long username widened the
  entire page, and the truncation meant to prevent exactly that never got to run.
  `min-w-0` on that column in `app/(app)/layout.tsx` fixes it everywhere at once.
- **The governance journey's "co-lead signup timeout, not yet root-caused."** The invite
  link is the one page a stranger lands on cold. A tap before hydration submitted the
  form the way a browser does with no JavaScript — a GET to the same URL, reloading the
  page and silently discarding the request the person believed they had sent.
- **The food journey's Plan button.** It sat underneath its own date input at 360 px and
  could not be tapped.
</completed_work>

<remaining_work>

1. Commit `components/forms/join-request-form.tsx` — the `useEffect` that set a `ready`
   flag is now `useSyncExternalStore`, because the React compiler lint rule rejects
   setState inside an effect. Typechecks and lints clean. **Stage that single path.**
2. Update `PROGRESS.md`: rewrite the phase-14 section (it still says the nav shell, Home,
   Today and Calendar are "not yet built"), flip the phase-status table row from
   "in progress" to built, and refresh the verification-state table — 794 tests, and the
   three e2e failures it lists as open are now closed.
3. Record the deliberate spec deviations in `DECISIONS.md`: Approvals in the bar at every
   width, and `/money` and `/dashboard` as redirects rather than renames.
4. Once the tree builds again, re-run the full Playwright suite to confirm 70/70.
</remaining_work>

<decisions_made>

- **Approvals enters the bar at every width, not only from 640 px up.**
  `docs/08-UI-UX-SPEC.md` section 3.1 caps the bar at six items *and* says never to drop
  Approvals while something is pending. The phase's own acceptance criterion settles the
  contradiction in favour of the pending queue. The reasoning is written into
  `components/layout/nav.tsx` so the next reader does not think it a mistake.
- **The Insights slot points at `/analytics` until phase 15 ships.** Insights supersedes
  analytics; linking a slot at a route that does not exist would break the phase's own
  "no screen reachable only from a URL" criterion in the other direction.
- **`/money` and `/dashboard` redirect rather than rename.** `/expenses` has been the
  ledger since phase 2 and carries approvals, recurring, close and settle beneath it.
  Renaming the family would break every link a Home has already shared.
- **Announcement severity is text with a check constraint, not an enum** — matching the
  schema in `docs/04-DATABASE.md` section 4. Three values, nothing in SQL branches on them.
- **`announcement_expires_in_the_future` holds on UPDATE too**, so an expiry cannot be
  back-dated. An announcement ends by expiring or by being taken down; rewriting history
  so it "was never shown" is neither.
- **Today asks the library recommender only, never the AI half.** The AI call is a second
  network hop with its own latency and failure mode, and Today is the screen that must
  not wait.
</decisions_made>

<blockers>
- **Slug collision blocks the app from starting** — `app/api/settlements/[period]/` versus
  the existing `[id]`. Needs the phase-15 owner. Nothing runs until it is resolved.
- **Concurrent writer in the same tree** — see the constraints above.
- **Docker/Supabase** — resolved this session by restarting Docker Desktop, but worth
  knowing: when it is down, every signup fails with `INTERNAL` / `TypeError: fetch failed`.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — the architecture boundaries and the non-negotiable domain rules.
2. `PROGRESS.md` phase-14 section — currently **stale**; it describes the nav shell, Home,
   Today and Calendar as unbuilt. All four are built. Fixing it is task 2 above.
3. `docs/08-UI-UX-SPEC.md` sections 3 and 4.12 — the navigation rules and S-50/S-51/S-52.
4. `docs/07-ROADMAP.md` phase 14 — the acceptance criteria the journey walks.
5. `components/layout/nav.tsx` header comment — why Approvals overrides the six-item cap.

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: `git add -A` here → stage explicit paths; a second writer shares this tree.
- [ANTI-PATTERN]: reading the red typecheck as this session's → filter out `insights`; zero errors remain.
- [ANTI-PATTERN]: adding a Playwright wait to a failing journey → probe the DOM first; all three failures this session were real defects.

## Infrastructure State

- **Local Supabase**: running. API on the port `.env.local` names; migrations 045–089
  applied; generated types match. `supabase_imgproxy`, `supabase_edge_runtime` and
  `supabase_pooler` are stopped — only Edge Functions need the second of those.
- **Docker Desktop**: running, restarted mid-session after it stopped on its own.
- **Dev server**: not running. It cannot start until the slug collision is fixed.
- **Migration 089** (`house_announcements`) is applied **locally only**. It has not been
  pushed to the hosted project, and pushing is a separately requested action.
- **Playwright config**: now 60 s timeout and one retry locally, both for `next dev`'s
  on-demand compilation rather than for anything the product does.
- **Scratchpad**: `…/scratchpad/parked/settlements-period` holds the copy of the
  phase-15 route that was parked and restored. `…/scratchpad/phase15-wip/insights` holds
  an earlier snapshot of `lib/domain/insights/` from before the concurrent writer
  recreated it. Neither is authoritative.

<context>
The instruction for this session was to build every phase except 15, then do a
repository-wide quality pass. Phases 10–13 were already built; phase 14 was the work.

It was built in the order `PROGRESS.md` had already argued for — Home overview, then
Today, then Calendar, then the nav shell, then the journey — so that the navigation never
pointed at a route that did not exist yet. Each slice is one commit, and each was
typechecked, linted, built and tested before the next began.

The pure decisions were pushed into `lib/domain/home/` and unit-tested without a
database: what "Needs you" contains and in what order, who counts as present, which
pending queues a caller may see, who owes whom, and the Calendar's rollups. The reads
were composed once in `lib/data/` so the screens and the endpoints cannot disagree — the
Today page and `GET /api/today` return the same object.

The quality pass then went after the three e2e failures `PROGRESS.md` listed as open,
and all three turned out to be real product defects rather than test flakiness. That is
the part worth carrying forward: the 360 px fix in particular was one class name in the
shell that had been silently disabling every `truncate` in the app.

Work stopped because the tree stopped building — a concurrent worker's phase-15 route
collides with an existing dynamic segment, and Next will not boot.
</context>

<next_action>
Start with: `git add components/forms/join-request-form.tsx` (that single path — not `-A`)
and commit it, then rewrite the phase-14 section of `PROGRESS.md` to match what is
actually built. Before running anything, settle the
`app/api/settlements/[period]` versus `[id]` collision with whoever owns phase 15.
</next_action>
